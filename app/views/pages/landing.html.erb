<script>
  // Add event listener immediately and another one on DOMContentLoaded
  function applyHoverEffects() {
    const featureBoxes = document.querySelectorAll('.card.bg-base-200');
    
    featureBoxes.forEach(box => {
      // First remove any existing listeners to avoid duplicates
      box.removeEventListener('mouseenter', addGlow);
      box.removeEventListener('mouseleave', removeGlow);
      
      // Now add the listeners
      box.addEventListener('mouseenter', addGlow);
      box.addEventListener('mouseleave', removeGlow);
    });
  }
  
  function addGlow(event) {
    this.style.boxShadow = '0 0 20px #5A4E9D';
    this.style.border = '2px solid #F4C430';
    this.style.transform = 'translateY(-5px)';
  }
  
  function removeGlow(event) {
    this.style.boxShadow = '';
    this.style.border = '';
    this.style.transform = '';
  }
  
  // Apply immediately
  applyHoverEffects();
  
  // Also apply when DOM is fully loaded
  document.addEventListener('DOMContentLoaded', applyHoverEffects);
  
  // Also apply after Turbo navigation
  document.addEventListener('turbo:load', applyHoverEffects);
</script>

<section class="flex items-center justify-center">
  <div class="py-10 mx-auto max-w-7xl">
    <div class="max-w-xl mx-auto">
      <div class="text-center">
        <div class="relative flex flex-col items-center">
          <div class="faithseal-logo-landing">
            <%= render 'shared/logo', width: '400', height: '400' %>
          </div>
          <h1 class="text-6xl font-bold mt-10 mb-6">
            FaithSeal
          </h1>
          <% if Docuseal.version.present? %>
            <span class="badge badge-outline badge-lg block mx-auto">
              v<%= Docuseal.version %>
            </span>
          <% end %>
        </div>
        <h2 class="mt-6 mb-16 text-xl text-center text-gray-600">
          A faith-based web platform that provides secure and efficient digital document signing and processing — powered by Scripture.
        </h2>
      </div>
    </div>
    <div class="grid grid-cols-1 gap-12 md:gap-10 md:grid-cols-2">
      <div class="card bg-base-200" style="transition: all 0.3s ease-in-out;">
        <div class="card-body">
          <div class="text-center transition-all">
            <div class="inline-block p-4 mb-4 -mt-16 bg-base-content rounded-full">
              <%= svg_icon('brand_docker', class: 'w-10 h-10 text-base-100 stroke-1') %>
            </div>
            <h3 class="mb-4 text-2xl font-semibold">Easy to Start</h3>
            <p class="text-base text-gray-500">
              Run on your own host using Docker container, or deploy on your favorite managed PaaS with a single <a href="https://www.faithseal.com/install" class="link link-neutral font-bold">click</a>.
            </p>
          </div>
        </div>
      </div>
      <div class="card bg-base-200" style="transition: all 0.3s ease-in-out;">
        <div class="card-body">
          <div class="text-center transition-all">
            <div class="inline-block p-4 mb-4 -mt-16 bg-base-content rounded-full">
              <%= svg_icon('devices', class: 'w-10 h-10 text-base-100') %>
            </div>
            <h3 class="mb-4 text-2xl font-semibold">Mobile Optimized</h3>
            <p class="text-base text-gray-500">
              Review and sign digital documents online from any device.
              FaithSeal document forms are optimized for screens of all sizes.
            </p>
          </div>
        </div>
      </div>
      <div class="card bg-base-200" style="transition: all 0.3s ease-in-out;">
        <div class="card-body">
          <div class="text-center transition-all">
            <div class="inline-block p-4 mb-4 -mt-16 bg-base-content rounded-full">
              <%= svg_icon('shield_check', class: 'w-10 h-10 text-base-100') %>
            </div>
            <h3 class="mb-4 text-2xl font-semibold">Secure</h3>
            <p class="text-base text-gray-500">
              Host it on your hardware under a VPN to ensure that important documents can be accesses only within your organization.
            </p>
          </div>
        </div>
      </div>
      <div class="card bg-base-200" style="transition: all 0.3s ease-in-out;">
        <div class="card-body">
          <div class="text-center transition-all">
            <div class="inline-block p-4 mb-4 -mt-16 bg-base-content rounded-full">
              <%= svg_icon('writing_sign', class: 'w-10 h-10 text-base-100') %>
            </div>
            <h3 class="mb-4 text-2xl font-semibold">Faith-Based</h3>
            <p class="text-base text-gray-500">
              A Scripture-inspired approach to document signing.<br>
              Faith-based document solution for your organization.
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
<%= render 'shared/attribution', with_counter: true %>
